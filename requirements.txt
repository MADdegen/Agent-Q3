# ── Core ─────────────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.28.0
pydantic>=2.10.0
pydantic-settings>=2.7.0

# ── AI / LLM clients ─────────────────────────────────────────────────────────
anthropic>=0.52.0
openai>=1.70.0              # used for HF router (OpenAI-compat) + OpenRouter

# ── Search tools (ported from perplexity-v.1) ────────────────────────────────
# Perplexity Sonar — deep research (Reasoner) + doc lookup (Coder)
# (called via httpx directly — no SDK required)

# Exa semantic search
exa-py>=1.3.0

# Tavily fast search
tavily-python>=0.5.0

# ── Database + cache ─────────────────────────────────────────────────────────
asyncpg>=0.30.0
redis[asyncio]>=5.2.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0
httpx                       # also used as test client

# ── Lint / type ───────────────────────────────────────────────────────────────
ruff>=0.9.0
mypy>=1.14.0
types-requests>=2.32.0
