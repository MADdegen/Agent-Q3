[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "/start.sh"
healthcheckPath = "/health"
healthcheckTimeout = 900
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "ollama-agent-q3"

[deploy.env]
PORT = "${PORT}"
